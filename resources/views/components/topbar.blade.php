<nav class="navbar rounded-4 px-3 mb-4"
    style="border:3px solid #000;background:rgba(255,255,255,.65);backdrop-filter:blur(6px)">
    <a class="navbar-brand fw-bold neon" href="{{ route('home') }}">Manicolandia 🎭💥</a>
    <div class="d-flex gap-2 ms-auto">
        <a class="btn btn-warning fw-bold" href="{{ route('home') }}">Home</a><a class="btn btn-info fw-bold"
            href="{{ route('contact.create') }}">Contatti</a>
    </div>
</nav>
